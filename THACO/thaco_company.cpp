#include <bit/stdc++.h>

using namespace std;

const int N = 3e5 + 5;

int n, val[N];

int main() {
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        scanf("%d", val + i);
    }
    for(int i = 2; i <= n; i++) {
        scanf("%d", )
    }

    return 0;
}