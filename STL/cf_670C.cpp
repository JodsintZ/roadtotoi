#include <bits/stdc++.h>

using namespace std;

int n,a[200005],m;
pair<int,int> p;
#define x first
#define y second

int main() {
    scanf("%d", &n);
    for (int i = 1; i <= n; i++) {
        scanf("%d", &a[i]);
    }
    scanf("%d", &m);
    for (int i = 1; i <= m; i++) {
        cin >> p.x;
    }
    for (int i = 1; i <= m; i++) {
        cin >> p.y;
    }
}
