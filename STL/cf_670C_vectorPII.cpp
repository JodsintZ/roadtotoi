#include <bits/stdc++.h>

using namespace std;

#define x first
#define y second

const int N = 200005;
int n, a[N], m;
vector <pair<int,int> > V, res;
//V.emplace_back(a, b);

int main() {
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &temp1);
        a[temp]++;
    }
    scanf("%d", &m);
    for (int i = 0; i < m; i++) {
        cin >> p.x;
    }
    for (int i = 0; i < m; i++) {
        cin >> p.y;
    }
   // sort(a,a+n);
    res = make_pair(0,0);
    for (int i = 1; i < N; i++ ){
        if(res < make_pair(p.x,p.y)) {

        }
    }


}
